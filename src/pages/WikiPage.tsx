// Wiki section removed. The Library (/library) is now the compound reference,
// and a purpose-built HomePage is the site landing page. This stub is intentionally
// empty and is not imported anywhere; kept only to avoid a dangling file reference.
export default function WikiPage() {
  return null;
}
